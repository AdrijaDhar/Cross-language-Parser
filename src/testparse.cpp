#include <iostream>
// Include C++11/14/17 standard library header files
#include <thread>

// Include header files for system-specific functions or types
#include <unistd.h>  // For POSIX system calls

using namespace std;
int main(){
    int A;
    A=12;
    cout<<sqrt(A)<<endl;
    return 0;
}
